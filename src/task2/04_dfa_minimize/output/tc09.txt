=== tc09.txt ===
Input DFA: 5 states, alphabet = {a,b}, initial=q0
Accessible: {q0,q1,q2,q3,q4}
Inaccessible: {}
mark() procedure:
S2: Initial marking:
    mark(q0, q3)
    mark(q1, q3)
    mark(q2, q3)
    mark(q3, q4)
    mark(q3, dead)
  Pass 1:
    mark(q0, q1) via a
    mark(q0, q2) via a
    mark(q1, q4) via a
    mark(q1, dead) via a
    mark(q2, q4) via a
    mark(q2, dead) via a
  Pass 2:
    mark(q0, q4) via a
    mark(q0, dead) via a
  Pass 3:
    (stable)

Indistinguishable: (q1,q2)
=== Minimized DFA ===
States: 4
Transition table:
Class		States		on a		on b		
----------------------------------------------------------------------
C0(init)		{q0}		C1		C1		
C1		{q1,q2}		C2		dead		
C2(final)		{q3}		C3		dead		
C3		{q4}		C3		C3		

Initial state: C0
Final states: C2 

